// Template literals


function printDate(sname, age){
    let toPrint= "student's anme is " +sname + "and age is: " +age;
    console.log(printDate);
}

let sname="aiman";
let age= 28;

printDate(sname,age);
